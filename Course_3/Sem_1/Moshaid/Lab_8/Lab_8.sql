-- Создание таблицы migrate_users
CREATE TABLE migrate_table (
    name VARCHAR(50),
    age int
);

-- Вставка данных в таблицу
INSERT INTO migrate_table (name, age) VALUES ('John', 25);
INSERT INTO migrate_table (name, age) VALUES ('Jane', 30);
-- Добавьте дополнительные строки по вашему усмотрению

-- Проверка данных в таблице
SELECT * FROM -- Создание таблицы migrate_users
CREATE TABLE migrate_table (
    name VARCHAR(50),
    age int
);

-- Вставка данных в таблицу
INSERT INTO migrate_table (name, age) VALUES ('John', 25);
INSERT INTO migrate_table (name, age) VALUES ('Jane', 30);
-- Добавьте дополнительные строки по вашему усмотрению

-- Проверка данных в таблице
SELECT * FROM migrate_table;
;
									